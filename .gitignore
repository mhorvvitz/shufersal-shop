node_modules/
*.log
.env
dictionary-draft.json

# Local secrets for `wrangler dev` (gateway/worker). Real secrets live in
# `wrangler secret put`, never in the repo.
.dev.vars

# Wrangler's local build/state cache.
.wrangler/

# Personal scan cache for the suggester (like the dictionary, reveals habits).
order-stats.json

# Your real dictionary is personal (reveals your shopping habits) — keep it local.
# product-dictionary.sample.json is the tracked, shareable starter.
product-dictionary.json

# Internal design/spec docs from the superpowers workflow — kept locally, not published.
docs/superpowers/
